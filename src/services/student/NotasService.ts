import { StudentDTO } from "../../models/dto/StudentDTO";

export class NotasService {
  /**
   * Consultar en la base de datos
   * consultar notas
   * devolver foto
   */

  public notas(student:StudentDTO): string {
    return 'consultar notas'
  }

}